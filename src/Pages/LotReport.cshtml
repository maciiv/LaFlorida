@page
@model LaFlorida.Pages.LotReportModel
@{
    ViewData["Title"] = "Reporte por Lotes";
}

<h1>Reporte por Lotes</h1>

<div class="row">
    <partial name="_SummaryReportPartial" model="@Model.SummaryStatistics" />
    <div class="col-md-6 col-12 my-3">
        <h4 class="my-3">Detalles por lote</h4>
        <form class="form-inline" method="post" asp-page-handler="GenerateReport" data-ajax="true" data-ajax-method="post" data-ajax-update="#report" data-ajax-loading="#spinner">
            <div asp-validation-summary="All" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="Lot.LotId" class="control-label mx-2"></label>
                <select asp-for="Lot.LotId" class="form-control" asp-items="ViewBag.LotId"></select>
                <span asp-validation-for="Lot.LotId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Ver Reporte" class="btn btn-primary mx-2" />
                <span id="spinner" style="display:none;"><i class="fas fa-spinner fa-spin"></i></span>
            </div>
        </form>
    </div>
    <div class="col-md-12" id="report">
        <partial name="_LotReportPartial" model="@Model.CycleStatistics" />
    </div>
</div>