@page
@model LaFlorida.Pages.ApplicationRoles.IndexModel
@{
    ViewData["Title"] = "Roles";
}

<h1>Roles</h1>

@if (Model.Success)
{
    <div class="alert alert-success" role="alert">
        @Model.Message
    </div>
}

@if (Model.Error)
{
    <div class="alert alert-danger" role="alert">
        @Model.Message
    </div>
}

<div class="row">
    <div class="col-md-12 my-3">
        <a asp-page="Create" class="btn btn-primary float-right">Nuevo Role</a>
    </div>
    <div class="col-md-12">
        <div class="table-responsive">
            <table class="table table-hover data-table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.ApplicationRoles[0].Name)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.ApplicationRoles)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Name)
                            </td>
                            <td>
                                <a asp-page="./Edit" asp-route-id="@item.Id"><i class="fas fa-pencil-alt text-primary" title="Editar"></i></a>
                                <a asp-page="./Delete" asp-route-id="@item.Id"><i class="fas fa-trash-alt text-danger" title="Borrar"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

