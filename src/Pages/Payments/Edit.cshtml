@page
@model LaFlorida.Pages.Payments.EditModel

@{
    ViewData["Title"] = "Editar Pago";
}

<h1>Editar</h1>

<h4>Pago</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="All" class="text-danger"></div>
            <input type="hidden" asp-for="Payment.PaymentId" />
            <input type="hidden" asp-for="Payment.CreateDate" />
            <div class="form-group">
                <label asp-for="Payment.Quantity" class="control-label"></label>
                <input asp-for="Payment.Quantity" class="form-control" />
                <span asp-validation-for="Payment.Quantity" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Payment.CycleId" class="control-label"></label>
                <select asp-for="Payment.CycleId" class="form-control" asp-items="ViewBag.CycleId"></select>
                <span asp-validation-for="Payment.CycleId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Payment.ApplicationUserId" class="control-label"></label>
                <select asp-for="Payment.ApplicationUserId" class="form-control" asp-items="ViewBag.ApplicationUserId"></select>
                <span asp-validation-for="Payment.ApplicationUserId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    @if (Model.DashboardId == 0)
    {
        <a asp-page="Index">Regresar</a>
    }
    else
    {
        <a asp-page="/Dashboard" asp-route-id="@Model.DashboardId">Regresar</a>
    }
</div>

