@page
@model LaFlorida.Pages.Jobs.IndexModel

@{
    ViewData["Title"] = "Trabajos";
}

<h1>Trabajos</h1>

@if (Model.Success)
{
    <div class="alert alert-success" role="alert">
        @Model.Message
    </div>
}

@if (Model.Error)
{
    <div class="alert alert-danger" role="alert">
        @Model.Message
    </div>
}

<div class="row">
    <div class="col-md-12 my-3">
        <a asp-page="Create" class="btn btn-primary float-right">Nuevo Trabajo</a>
    </div>
    <div class="col-md-12">
        <div class="table-responsive">
            <table class="table table-hover data-table">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.Jobs[0].Name)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Jobs[0].IsRent)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.Jobs[0].IsMachinist)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.Jobs)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.Name)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.IsRent)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.IsMachinist)
                            </td>
                            <td>
                                <a asp-page="./Edit" asp-route-id="@item.JobId"><i class="fas fa-pencil-alt text-primary" title="Editar"></i></a>
                                <a asp-page="./Delete" asp-route-id="@item.JobId"><i class="fas fa-trash-alt text-danger" title="Borrar"></i></a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
